# This branch always starts its own stack, including Qdrant and Ollama.
# Legacy RAG_* environment variables intentionally do not configure this stack.
name: codex-rag-chunking
include:
  - path: compose.chunking.yaml

# HTTPS enablement — student1-research-rag

User authorization on 2026-09-23: “เปิด https เลย”, following the explicit question about opening this Research-RAG endpoint without login and disclosure that tools can modify/delete workspace data. This authorizes public no-auth MCP for the classroom. No firewall/IAM/billing change is included; the user still manages network tags/firewall.

Verified before state: VM student1-research-rag in bigdatainpractice1, asia-southeast1-a, IP 34.142.222.110, RUNNING, no network tags, no reserved addresses. Existing private MCP healthy at 127.0.0.1:9076/mcp; review on 127.0.0.1:9077; database/model ports unpublished. No Caddy installed yet.

Intended hostname: `34-142-222-110.sslip.io`; local DNS, Google 8.8.8.8 and Cloudflare 1.1.1.1 all resolve it to the actual IP. IP remains ephemeral; stop/start may require a changed hostname until the user reserves the address. No domain purchase or extra managed compute/database is needed.

Intended changes: pull official Caddy 2 Alpine image, capture/pin its immutable digest, store its config/certificates on /srv/research-rag, configure HTTPS routing only /mcp and /mcp/* to actual loopback MCP, set the existing app's RAG_PUBLIC_ORIGIN to the exact HTTPS origin, recreate only MCP for that environment change. Keep review, Qdrant and Ollama private. Caddy admin endpoint disabled. Do not record certificate private keys or ACME account secrets.

External TCP80/443 require the existing http-server and https-server tags. Asked the user to add these manually before certificate issuance/public verification. Do not modify GCP firewall rules or use a tunnel to bypass missing ingress.

Verification: check trusted TLS from outside VM; real native MCP initialize, tools/list, workspace_status; compare public catalog and state against local endpoint; test allowed Origin and rejected unapproved Host/Origin using the existing verifier. Do not claim Claude UI integration without a real learner/client test. No mocks or protocol translation.

Cost: same running VM and existing disks; additional Caddy image/cert storage and network traffic. No new VM, disk, static address or load balancer. Rollback: stop/remove only the new edge container and restore the previous RAG_PUBLIC_ORIGIN with a logged MCP recreate; preserve data and TLS keys on the data disk. Results pending.

## Preparation outcome

[Logged preparation](20260923T155429Z-7bf937.md) succeeded: official Caddy image pinned to `caddy@sha256:6aeddd44c3078b0f9a35206472a11420648a79c184603ef95957d0a20044cb2b`; Caddy configuration validated; MCP recreated with exact RAG_PUBLIC_ORIGIN and passed health check. Caddy has not been started and no certificate has been requested. Config/certificate directories are on the data disk; no private keys were read or copied.

Read-only firewall recheck confirms public HTTP/HTTPS rules require http-server/https-server tags; current VM tags remain absent. Only internal-network ingress is otherwise allowed. Public readiness is blocked on the user's manual network-tag change. No firewall rule or instance tag was changed by Codex.

Native external client prepared on the Mac: MCP SDK 2.2.0. External TLS and MCP tests remain pending ingress/certificate issuance; do not label the candidate URL usable yet.

## User enabled ingress tags

At 2026-09-23T15:56:38.172502+00:00 live instance inspection confirms `http-server` and `https-server`. The user confirmed “ทำล่ะ”; attribute this manual tag change to the user, not Codex. Proceed with the already authorized Caddy start and TLS issuance.

## Final verification — 2026-09-23T15:59:14.054679+00:00

Public endpoint `https://34-142-222-110.sslip.io/mcp` passed external native MCP SDK verification from the Mac: trusted TLS 1.3, initialize, 45 tools, workspace status/list, actual hybrid retrieval and two exact citation spans. Repository public/local verifier passed 8 checks. Caddy logs confirm successful Let's Encrypt issuance. HTTP308 redirects and root404 verified. Initial curl GET /mcp timed out because it opens an SSE stream; it returned HTTP200 and was not used as sole MCP readiness evidence.

Evidence: [external SDK](../docs/gcp/evidence/external-https-20260923.json), [public/local](../docs/gcp/evidence/public-crosscheck-20260923.json), [Caddy](../docs/gcp/evidence/caddy-https-20260923.txt). Caddy and original four app containers remain running. No firewall rule, IAM, VM, disk or address was created/modified by Codex in this HTTPS phase. User manually added tags. External IP is still unreserved. Claude UI has not been tested. All cloud mutations have timestamped sibling records.

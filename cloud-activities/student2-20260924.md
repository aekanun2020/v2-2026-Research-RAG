# Student 2 clone deployment — 24 September 2026

User explicitly requested checking and running `student2-research-rag` from the student1 machine image with equivalent services and HTTPS.

## Verified before state

- GCP project `bigdatainpractice1`; acting account `thaimcpagent@gmail.com`.
- User-created VM `student2-research-rag`, ID `4024488336236859568`, zone `asia-southeast1-b`, RUNNING, e2-standard-8, no GPU.
- Source machine image: `projects/bigdatainpractice1/global/machineImages/student1-research-rag`.
- Public IP `34.142.163.231`, private IP `10.148.0.36`. No static reservation for this IP was listed.
- Both http-server and https-server tags already present. Codex will not change firewall or tags.
- Separate boot/data disks `student2-research-rag` and `student2-research-rag-1` in zone b, 30/100 GiB; retained data disk. Not student1's disks.
- SSH ED25519 fingerprint verified against authenticated GCP serial output: `SHA256:YwYJHZfi20AEpnMiZbaxym+iHRoP1DZxQut48hr0DfM`.
- Planned URL: https://34-142-163-231.sslip.io/mcp — not yet verified.

## Intended work and cost

Inspect cloned files and services; preserve cloned application data; adapt only student2 public origin/Caddy hostname as required; start its services and verify real native MCP externally. First SSH may initialize an OS Login home. Record all mutations before execution with [student2 logger](../scripts/gcp/log_student2_action.py). No creation of further VMs or paid services is planned. Existing VM compute, disks and IP incur charges while running. HTTPS uses Caddy on this VM.

Initial status: BLOCKED at guest SSH; resolved after the user removed the attached service account and restarted the VM. Current result: HTTPS/native MCP verification passed; see completion below. Back up changed configuration before mutation; retain old files for rollback. Never alter student1.

## SSH blocker and evidence

- First strict-host-key SSH attempt returned `Permission denied (publickey)`; [record](20260924T002953Z-398612.md). No remote command executed and no guest mutation is established.
- Both student1 and student2 have `enable-oslogin=TRUE`. The same existing key and OS Login identity still successfully connected to student1.
- Unlike student1 (no attached service account), student2 attaches `65900660028-compute@developer.gserviceaccount.com`.
- IAM `testIamPermissions` on that service account for `iam.serviceAccounts.actAs` returned `{}` (permission not granted).
- Acting user's direct project roles: `roles/compute.osAdminLogin`, `roles/iap.tunnelResourceAccessor`, `roles/viewer`.
- These checks establish a missing OS Login prerequisite on student2. [Google OS Login role requirements](https://docs.cloud.google.com/compute/docs/oslogin/set-up-oslogin) require Service Account User on the attached service account.
- Required manual user action to continue without stopping the VM: grant `thaimcpagent@gmail.com` role `roles/iam.serviceAccountUser` scoped to `65900660028-compute@developer.gserviceaccount.com`, then retry the original SSH command. No project-wide role grant is needed for this prerequisite. This grants ability to act as that service account, not just a machine-specific SSH permission.
- Alternative: user may remove the VM service account to match student1, which requires a stop/edit/start flow and can change the currently ephemeral IP. Do not silently perform this alternative.
- Codex has not modified IAM, VM power state, firewall, tags, application configuration or certificates. Guest state remains uninspected. Planned hostname must not be advertised as ready.

- Blocker recorded: 2026-09-24T00:31:00.230492+00:00

## Completion

The user manually removed the service account and restarted student2. Live GCP inspection showed RUNNING, no attached service account, unchanged IP `34.142.163.231`, existing http-server/https-server tags. The address remains ephemeral: the reservation list contains student1 only. Codex did not stop/start the VM, reserve an address, or alter IAM/firewall/tags.

The original failing SSH command then succeeded. All five cloned containers started on boot; `/srv/research-rag` mounted ext4 with 90 GiB available. Root cause of the HTTPS mismatch: cloned Caddyfile and `RAG_PUBLIC_ORIGIN` both still named student1's hostname. A verified TLS connection to student2's hostname failed with `TLSV1_ALERT_INTERNAL_ERROR` before correction.

Codex backed up both files under `/srv/research-rag/deploy/backup-student2-20260924/` (directory mode 700), replaced only the hostname with `34-142-163-231.sslip.io`, validated Caddy, recreated MCP with the new origin, and recreated Caddy. Existing source, pinned images, volumes and application data were preserved. No student1 mutation was performed. Caddy's TLS storage remains persistent on the cloned data disk.

Verified endpoint: **https://34-142-163-231.sslip.io/mcp**. Public access has the same no-login design as the requested student1 service. Learners create their own workspace and use the returned workspace_id; machine-image data includes the earlier test workspaces, which were retained.

### Results

- External native MCP: verified TLS 1.3, initialize/tools/list, 45 tools, workspace_status/list_workspaces, real hybrid retrieval, two exact source-page citation spans.
- Public-vs-local cross-check: 8 checks passed, including matching catalogs/default workspace, acceptance of student2's public Origin, rejection of unapproved Host (421) and Origin (403).
- All five containers running, MCP/review healthy. Images match the recorded student1 deployment. All restart policies are unless-stopped; Docker/containerd enabled. No post-change VM power cycle was initiated.
- No Claude Desktop UI test or fresh load test was performed. No semantic answer grading or external model calls were used.

### Evidence and actions

- [Successful SSH retry and before-state runtime](20260924T003523Z-fe3c2b.md)
- [HTTPS failure before correction](../docs/gcp/evidence/student2-before-https-20260924.json)
- [Configuration correction activity](20260924T003608Z-8fc4c4.md)
- [Exact correction script](../scripts/gcp/configure-student2-https.sh)
- [External TLS and retrieval result](../docs/gcp/evidence/student2-external-https-20260924.json)
- [Public-vs-local check activity](20260924T003657Z-a9c095.md)
- [Full public-vs-local result](../docs/gcp/evidence/student2-public-crosscheck-20260924.json)
- [Final runtime and image/restart-policy snapshot](../docs/gcp/evidence/student2-runtime-20260924.txt)

### Rollback and cost

No rollback was needed. Pre-change Caddyfile/runtime.env copies are available at the backup directory; restoring them would restore the inherited student1 hostname and would not provide a working student2 URL. Recreate the affected containers after an authorized restoration. Do not alter student1.

No new VM, load balancer, reserved address or paid certificate was created. Existing VM/disk/IP charges continue. Reserve the current IP manually if the endpoint must survive stop/start without a hostname change.

- Completed verification recorded: 2026-09-24T00:37:46.578221+00:00

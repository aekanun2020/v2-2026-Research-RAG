# Student 2 clone deployment — 24 September 2026

User explicitly requested checking and running `student2-research-rag` from the student1 machine image with equivalent services and HTTPS.

## Verified before state

- GCP project `bigdatainpractice1`; acting account `thaimcpagent@gmail.com`.
- User-created VM `student2-research-rag`, ID `4024488336236859568`, zone `asia-southeast1-b`, RUNNING, e2-standard-8, no GPU.
- Source machine image: `projects/bigdatainpractice1/global/machineImages/student1-research-rag`.
- Public IP `34.142.163.231`, private IP `10.148.0.36`. No static reservation for this IP was listed.
- Both http-server and https-server tags already present. Codex will not change firewall or tags.
- Separate boot/data disks `student2-research-rag` and `student2-research-rag-1` in zone b, 30/100 GiB; retained data disk. Not student1's disks.
- SSH ED25519 fingerprint verified against authenticated GCP serial output: `SHA256:YwYJHZfi20AEpnMiZbaxym+iHRoP1DZxQut48hr0DfM`.
- Planned URL: https://34-142-163-231.sslip.io/mcp — not yet verified.

## Intended work and cost

Inspect cloned files and services; preserve cloned application data; adapt only student2 public origin/Caddy hostname as required; start its services and verify real native MCP externally. First SSH may initialize an OS Login home. Record all mutations before execution with [student2 logger](../scripts/gcp/log_student2_action.py). No creation of further VMs or paid services is planned. Existing VM compute, disks and IP incur charges while running. HTTPS uses Caddy on this VM.

Status: BLOCKED at guest SSH; HTTPS not configured or verified. Back up changed configuration before mutation; retain old files for rollback. Never alter student1.

## SSH blocker and evidence

- First strict-host-key SSH attempt returned `Permission denied (publickey)`; [record](20260924T002953Z-398612.md). No remote command executed and no guest mutation is established.
- Both student1 and student2 have `enable-oslogin=TRUE`. The same existing key and OS Login identity still successfully connected to student1.
- Unlike student1 (no attached service account), student2 attaches `65900660028-compute@developer.gserviceaccount.com`.
- IAM `testIamPermissions` on that service account for `iam.serviceAccounts.actAs` returned `{}` (permission not granted).
- Acting user's direct project roles: `roles/compute.osAdminLogin`, `roles/iap.tunnelResourceAccessor`, `roles/viewer`.
- These checks establish a missing OS Login prerequisite on student2. [Google OS Login role requirements](https://docs.cloud.google.com/compute/docs/oslogin/set-up-oslogin) require Service Account User on the attached service account.
- Required manual user action to continue without stopping the VM: grant `thaimcpagent@gmail.com` role `roles/iam.serviceAccountUser` scoped to `65900660028-compute@developer.gserviceaccount.com`, then retry the original SSH command. No project-wide role grant is needed for this prerequisite. This grants ability to act as that service account, not just a machine-specific SSH permission.
- Alternative: user may remove the VM service account to match student1, which requires a stop/edit/start flow and can change the currently ephemeral IP. Do not silently perform this alternative.
- Codex has not modified IAM, VM power state, firewall, tags, application configuration or certificates. Guest state remains uninspected. Planned hostname must not be advertised as ready.

- Blocker recorded: 2026-09-24T00:31:00.230492+00:00

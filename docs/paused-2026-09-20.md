# Paused at user request — 20 September 2026

**Historical checkpoint.** The user subsequently requested resumption. Implementation resumed and 0.2.0 was deployed at 13:00 ICT on the same date; [current runtime](semantic-runtime-2026-09-20.json), [final tests](semantic-final-suite-2026-09-20.txt), and [retrieval assessment](semantic-assessment-2026-09-20.md) supersede the status below. No pause remains in force.

Recorded: 2026-09-20T04:48:57.175979+00:00

User requested a temporary pause because the machine must be shut down. Stop all implementation until the user resumes. The task is **not complete**. No new deployment or commit was performed at pause.

## Verified target and requested work

Standalone project: `codex-research-rag-mcp` in this repository. Finish the eight-stage research MCP over Streamable HTTP, Docker, semantic/hybrid retrieval, document/version/chunk identities and chunk management, retaining the manuscript identity feature already deployed. No cloud or remote Mac Studio deployment is in scope. CPU only; no external model judge, mocks, proxies or silent fallback.

## Deployed version remains unchanged

The earlier Docker image `codex-research-rag-mcp:0.1.0` remains the deployed version; its retrieval is lexical. MCP endpoint was `http://127.0.0.1:8776/mcp`, review port 8777. Bearer token is in ignored `.data/.http-token`; do not print or commit it. The two Compose services mount `.data`; restart policy is unless-stopped. No containers were changed during this semantic implementation or pause. Recheck live state after reboot; do not assume services have restarted.

Previously deployed image: `sha256:2eb281b12c84a4009d2e2080fdb66203e0d0591a5e6cb089fccf29e10a2604c5`. Previous 21-test results concern that earlier version, **not** the unfinished work below. Last production workspace check before this implementation was revision 0 and empty; recheck before any migration/deploy. Preserve unrelated containers.

## Work saved on disk, not yet deployed

- Added pinned CPU embedding dependencies and updated `uv.lock`: numpy 2.3.5, onnxruntime 1.30.0, tokenizers 0.23.2.
- Downloaded and SHA-256 verified original `intfloat/multilingual-e5-small` revision `614241f622f53c4eeff9890bdc4f31cfecc418b3`. Assets are ignored under `.models/multilingual-e5-small` (~487 MB); download script and manifest are in source. Original model metadata/card/config and PyPI metadata are under `third-party/embedding-model/`.
- Added `semantic.py`: explicit CPUExecutionProvider, local ONNX, query/passage prefixes, masked mean pooling, normalization, overflow windows, vector integrity checks, exact cosine. It runs successfully on this Mac. ONNX runtime emitted sandbox cache/telemetry persistence warnings; investigate implications if needed, do not describe them as inference failures. Telemetry is disabled via runtime API.
- Added embeddings schema and Store lexical/semantic/hybrid ranking, automatic indexing on import. Default Store search is hybrid; MCP tool signature/docstrings have **not yet been updated**.
- Added `documents.py` with logical document IDs, source versions, text revision IDs, chunk sets/status, context, coverage checks, evidence-usage lookup and index management. Wired Store migration/import/search and artifact evidence warnings. This last chunk-management change has passed Python compilation only; **no behavioral tests yet**.
- `.venv` was synced before the latest source edits. Use `PYTHONPATH=src` for development or resync installed code before testing; do not mistake old installed code for current source.

## Reproduced case and current result

[Before evidence](semantic-before-2026-09-20.json): real brms PDF, Thai query `การประมาณค่าแบบเบย์สำหรับข้อมูลที่มีโครงสร้างหลายระดับ`, lexical search returned zero hits.

[First semantic result](semantic-after-2026-09-20.json): semantic retrieval returns hits, but **Codex inspected them and did not accept relevance as passing**. Top results are predictor syntax on zero-based page 6 and bibliographic entries on page 24. Do not present this as a successful semantic-quality regression. Preserve this result before further changes.

Additional diagnostic (not a full ranking test): first 1200 characters from pages 0,1,2,3,6,24 scored respectively about `[.8096,.8002,.8075,.7991,.8045,.8137]` for the same Thai query. English `Bayesian estimation for multilevel data` scored `[.8755,.8388,.8293,.8342,.8145,.8322]`. Tokenization includes intact Thai tokens and expected query prefix; ONNX inputs are int64 input_ids, attention_mask, token_type_ids, and output shape is checked. Exact cause of weak Thai ranking remains unproven. Do not silently translate/change the original failing query to claim a pass.

## Resume in this order

1. Inspect current source and preserve existing evidence. Diagnose Thai retrieval quality using actual passages and original model recipe. Codex assesses relevance itself. Make one causal change per experiment; no unsupported quality claims.
2. Validate the new document/chunk implementation against actual PDF/source files: migration retaining old IDs, version linkage, latest-version default with explicit historical filters, candidate chunk sets/activation, exclusion, span citations/history, impacted manuscript reviews, index rebuild/integrity and backup/restore.
3. Expose document/chunk/index methods as typed MCP tools; add optional document_id to import and mode to retrieval. Update workspace capabilities and instructions. Existing 19-tool tests need the verified new count.
4. Add focused real integration regressions, rerun the original case, related cases, then full suite. Existing lexical-specific tests should explicitly select lexical mode. No fake paper or substitute server.
5. Package verified model assets in Docker with a pinned download stage and explicit model path. Current Dockerfile does not yet include them. Capture new dependency notices/provenance. Test the actual built container and real Streamable HTTP initialize/tools/list/tools/call.
6. Back up and inspect the actual local production workspace; preserve HTTP token and existing data. Update only these two local Compose services. Verify all other containers unchanged, health, actual tool calls and persistence. No remote host changes.
7. Update project/root README navigation, tool guide, design, ingestion-to-index flow, validation and deployment records. Verify links and authored whitespace (exclude verbatim upstream licenses). Report exact tested capability and remaining limitations in Thai.

## Process state at pause

Model download and uv sync exited successfully. Both one-off embedding diagnostic commands completed. No development/test/download command launched by this implementation remains pending. No further build, deployment or tests were started after the user requested pause. Existing Docker services were not stopped or altered.

# HTTPS for the first classroom machine

Status: **prepared, not publicly available yet**. User explicitly authorized opening HTTPS after disclosure of the current no-login design.

Candidate endpoint: `https://34-142-222-110.sslip.io/mcp`. DNS resolves to the actual VM IP using the local resolver, 8.8.8.8 and 1.1.1.1. The address is not reserved static, so stopping/starting the VM could change this URL.

Caddy is configured in `/srv/research-rag/edge/compose.yaml` with image digest `sha256:6aeddd44c3078b0f9a35206472a11420648a79c184603ef95957d0a20044cb2b`. It will route only /mcp and /mcp/* to `127.0.0.1:9076`. Review remains private on 9077; database/model ports remain unpublished. Caddy admin API is disabled and TLS data stays on the data disk. MCP is healthy with `RAG_PUBLIC_ORIGIN=https://34-142-222-110.sslip.io`.

## Pending user-managed ingress

On VM `student1-research-rag`, Edit → Network tags: add `http-server` and `https-server`, then Save. The existing default-allow-http/default-allow-https rules already allow TCP80/443 to those tags. Codex has not changed firewall rules or tags. Current tags are absent.

After live verification of these tags, start the prepared Caddy Compose through the activity logger, inspect certificate issuance, then verify public trusted TLS and real initialize/tools-list/tool-call from outside the VM. Also run the repository's public-vs-local verifier. Until these tests pass, do not distribute the candidate endpoint as ready.

- [Preparation script](../../scripts/gcp/prepare-https.sh)
- [Authorization, before/after state and cloud record](../../cloud-activities/https-20260923.md)
- [Private load-test results](readiness-20260923.md)
- [Caddy automatic HTTPS documentation](https://caddyserver.com/docs/automatic-https)

# HTTPS for the first classroom machine

Status: **HTTPS and native MCP verified from outside the VM**. User explicitly authorized opening HTTPS after disclosure of the current no-login design.

Verified endpoint: `https://34-142-222-110.sslip.io/mcp`. DNS resolves to the actual VM IP using the local resolver, 8.8.8.8 and 1.1.1.1. Rechecked on 24 September 2026: the address is reserved static as `student1-research-rag` and IN_USE by this VM. Codex did not create the reservation.

Caddy is configured in `/srv/research-rag/edge/compose.yaml` with image digest `sha256:6aeddd44c3078b0f9a35206472a11420648a79c184603ef95957d0a20044cb2b`. It will route only /mcp and /mcp/* to `127.0.0.1:9076`. Review remains private on 9077; database/model ports remain unpublished. Caddy admin API is disabled and TLS data stays on the data disk. MCP is healthy with `RAG_PUBLIC_ORIGIN=https://34-142-222-110.sslip.io`.

## User-managed ingress and verification

On VM `student1-research-rag`, Edit → Network tags: add `http-server` and `https-server`, then Save. The existing default-allow-http/default-allow-https rules already allow TCP80/443 to those tags. Codex has not changed firewall rules or tags. The user added both tags and Codex verified them live before starting Caddy.

Caddy was started after the user added tags. Let's Encrypt issued the public certificate successfully. External Mac native MCP SDK tests passed with TLS 1.3, 45 tools, workspace_status, list_workspaces, real hybrid retrieval and two exact source-span checks. The repository's public-vs-local verifier passed all 8 checks, including allowed public Origin and rejected unapproved Host/Origin. HTTP redirects to HTTPS with 308; non-MCP root path returns 404. A plain GET /mcp opens an SSE stream and can reach curl's timeout; native protocol tests are the readiness evidence.

This endpoint requires no login/token as authorized. Each learner must create and use their own returned workspace_id. No live Claude Desktop UI session was tested by Codex. The VM was observed running after boot on 24 September, retaining the reserved static IP. Other five group VMs are not deployed.

- [External native MCP and TLS evidence](evidence/external-https-20260923.json)
- [Public-vs-local cross-check](evidence/public-crosscheck-20260923.json)
- [Caddy certificate and container state](evidence/caddy-https-20260923.txt)

- [Preparation script](../../scripts/gcp/prepare-https.sh)
- [Authorization, before/after state and cloud record](../../cloud-activities/https-20260923.md)
- [Private load-test results](readiness-20260923.md)
- [Caddy automatic HTTPS documentation](https://caddyserver.com/docs/automatic-https)

- [24 September post-boot verification and retained-data checks](../../cloud-activities/20260924-readonly-after-boot.md)
